<UserControl
    x:Class="PrototipadoEscritorio.Views.EditarAccesorioModalUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PrototipadoEscritorio.ViewModels.Tienda"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:EditarAccesorioModalVM />
    </UserControl.DataContext>

    <!--  Overlay oscuro que cubre toda la pantalla  -->
    <Grid>

        <!--  Centrar el modal  -->
        <Border
            Width="420"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="White"
            BorderBrush="Transparent"
            BorderThickness="2"
            CornerRadius="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!--  Header verde  -->
                    <RowDefinition Height="Auto" />
                    <!--  Contenido  -->
                </Grid.RowDefinitions>

                <!--  ===== HEADER VERDE =====  -->
                <Border
                    Grid.Row="0"
                    Background="#2E7D32"
                    CornerRadius="10,10,0,0">
                    <TextBlock
                        Padding="24,16"
                        FontSize="20"
                        FontWeight="SemiBold"
                        Foreground="White">
                        <Run Text="Editar: " />
                        <Run Text="{Binding NombreOriginal}" />
                    </TextBlock>
                </Border>

                <!--  ===== CONTENIDO =====  -->
                <StackPanel Grid.Row="1" Margin="24,20,24,24">

                    <!--  NOMBRE  -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock
                            Margin="0,0,0,6"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="#333">
                            <Run Text="Nombre:" />
                        </TextBlock>
                        <Border
                            Background="#F5F5F5"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="6">
                            <TextBox
                                Padding="10,8"
                                Background="Transparent"
                                BorderThickness="0"
                                FontSize="14"
                                Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />
                        </Border>
                    </StackPanel>

                    <!--  PUNTOS  -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock
                            Margin="0,0,0,6"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="#333">
                            <Run Text="Puntos:" />
                        </TextBlock>
                        <Border
                            Background="#F5F5F5"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="6">
                            <TextBox
                                Padding="10,8"
                                Background="Transparent"
                                BorderThickness="0"
                                FontSize="14"
                                Text="{Binding Precio, UpdateSourceTrigger=PropertyChanged}" />
                        </Border>
                    </StackPanel>

                    <!--  IMAGEN  -->
                    <StackPanel Margin="0,0,0,20">
                        <TextBlock
                            Margin="0,0,0,6"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="#333">
                            <Run Text="Imagen:" />
                        </TextBlock>

                        <!--  Nombre de archivo + botón seleccionar  -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Margin="0,0,8,0"
                                Background="#F5F5F5"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1"
                                CornerRadius="6">
                                <TextBlock
                                    Padding="10,8"
                                    VerticalAlignment="Center"
                                    FontSize="13"
                                    Foreground="#555"
                                    Text="{Binding NombreArchivo}"
                                    TextTrimming="CharacterEllipsis" />
                            </Border>

                            <Button
                                Grid.Column="1"
                                Padding="12,8"
                                Command="{Binding SeleccionarImagenCommand}"
                                Content="Seleccionar img"
                                Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#F0F0F0" />
                                        <Setter Property="BorderBrush" Value="#BDBDBD" />
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="FontSize" Value="12" />
                                        <Setter Property="Foreground" Value="#333" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border
                                                        Padding="{TemplateBinding Padding}"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E0E0E0" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <!--  Preview de la imagen  -->
                        <Border
                            Width="120"
                            Height="120"
                            HorizontalAlignment="Left"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Image
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Source="{Binding Imagen}"
                                Stretch="Uniform" />
                        </Border>
                    </StackPanel>

                    <!--  BOTÓN GUARDAR  -->
                    <Button
                        Width="120"
                        Height="40"
                        HorizontalAlignment="Right"
                        Command="{Binding GuardarCommand}"
                        Content="Guardar"
                        Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#4CAF50" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="FontSize" Value="14" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border
                                                Padding="{TemplateBinding Padding}"
                                                Background="{TemplateBinding Background}"
                                                BorderThickness="0"
                                                CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#388E3C" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#2E7D32" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
